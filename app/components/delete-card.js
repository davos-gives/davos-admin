import Component from '@ember/component';

export default Component.extend({
  classNames: "flex flex-wrap justify-center w-screen h-screen",


    actions: {
      deleteVaultCard() {

        this.deleteVaultCard();
      }
    }

});
